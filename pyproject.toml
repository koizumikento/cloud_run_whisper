[project]
name = "cloud-run-whisper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "faster-whisper",
    "hypercorn",
    "uvicorn",
]
